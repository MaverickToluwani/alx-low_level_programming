#include <stdio.h>

/**
 * sun - Subtracts ome integer from another integer
 * @a: integer to be subtracted from
 * @b: integer to subtract from first integer
 * Return: result
 */
int sub(int a, int b)
{
	int res;
	res = a - b;
	return res;
}
