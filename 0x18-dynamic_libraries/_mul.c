#include <stdio.h>

/**
 * mul - Multiplies two numbers together
 * @a: Number to be multiplied to the second number
 * @b: Number to be multiplied to the first number
 * Return: result
 */
int mul(int a, int b)
{
	int res = a * b;
	return res;
}
